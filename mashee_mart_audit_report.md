# تقرير التدقيق الشامل — ماشي مارت (DDD Mart)

**نوع الفحص:** فحص شامل وعميق (Exhaustive Audit) لكافة أكواد وبنية المشروع، بناءً على محاكاة رحلات المستخدمين الفعلية (Customer / Cashier / Admin) وفحص الأمان والبنية التحتية (Supabase/RLS).

**المنهجية:** تم تشغيل 4 عملاء بحث مستقلين بالتوازي، كل واحد اختص برحلة/محور واحد وقرأ الكود الفعلي (خدمات، hooks، مكوّنات، migrations، سياسات RLS) سطراً بسطر قبل تسجيل أي ملاحظة. كل ملاحظة مذكورة بالأسفل مدعومة بملف وسطر فعلي تم فتحه والتحقق منه — لا توجد ملاحظات افتراضية أو تخمينية.

**لا يوجد أي تعديل على الكود في هذا الفحص** — هذا تقرير قراءة فقط (Read-only). يحتاج كل بند لقرار وتنفيذ منفصل.

---

## ملخص تنفيذي

| المستوى | العدد |
|---|---|
| 🔴 حرج (يستحق إصلاحاً عاجلاً) | 8 |
| 🟡 متوسط | 6 |
| 🟢 تحسين | 5 |

**أخطر 3 نقاط يُنصح بالبدء فيها فوراً:**

1. **استرجاع أموال بدون تحقق من السعر الأصلي + إمكانية استرجاع مزدوج** (رحلة الكاشير #3 و#4) — ثغرة احتيال مالي مباشر، كاشير يمكنه سحب مبالغ نقدية أكبر من قيمة البضاعة المرتجعة فعلياً.
2. **الكاشير يقدر يحذف/يعدّل أي منتج مباشرة عبر قاعدة البيانات** (الأمان #1 / رحلة المدير #1) — لأن صلاحيات القاعدة (RLS) لا تفرّق بين أدمن وكاشير على جدول المنتجات، فإخفاء الأزرار بالواجهة وحده لا يحمي شيء.
3. **دالة إنشاء المستخدمين الجديدة لا تتحقق من صحة رقم المتجر** (الأمان #1) — احتمال (مرتبط بإعدادات Supabase Auth) لأي شخص يسجّل حساب جديد أن يصبح "أدمن" مباشرة. يحتاج تحقق عاجل من إعدادات لوحة Supabase.

---

## 1. رحلة الزبون (Customer / Cart / Checkout Journey)

### 1.1 فقدان الكمية المعلّقة عند حذف/تفريغ السلة أثناء تشغيل مؤقت الدمج
- **المشكلة:** عند الضغط على "+/-" في السلة، تُجدوَل عملية تعديل فعلي للمخزون (RPC) بعد 200ms عبر `pendingQuantityRef`. إذا ضغط المستخدم "حذف" أو "تفريغ السلة" خلال هذه الـ 200ms، لا يُلغى المؤقت المعلّق، فيُنفَّذ لاحقاً على منتج لم يعد في السلة أصلاً — ما يسبب تعديلاً مضاعفاً وخاطئاً لرصيد المخزون.
- **الملف/السطر:** `hooks/usePOS.ts:146-238` (غياب تنظيف `pendingQuantityRef` في `removeItem` بالسطور 204-220 و`clear` بالسطور 222-238)
- **مستوى الخطورة:** 🔴 حرج
- **طريقة الإصلاح المقترحة:** في بداية `removeItem` و`clear`، ألغِ أي مؤقت معلّق بنفس الباركود (`clearTimeout`) واحذفه من الخريطة، مع دمج قيمته ضمن حساب كمية الاسترجاع الفعلية.

### 1.2 نفس المشكلة تتكرر عند "دفع" و"تعليق" الفاتورة
- **المشكلة:** `checkout` و`holdCurrentSale` يستدعيان `cart.clear()` بدون تنظيف المؤقتات المعلّقة، فيستمر أي مؤقت كمية معلّق بالعمل بعد اكتمال البيع ويُطبّق تعديل مخزون "يتيم" على منتج غير مرتبط بأي عملية حالية.
- **الملف/السطر:** `hooks/usePOS.ts:222-238`, `322-324`, `346-347`, `390`
- **مستوى الخطورة:** 🟡 متوسط
- **طريقة الإصلاح المقترحة:** إضافة دالة `flushPendingQuantityTimers()` تُلغي كل المؤقتات فوراً وتُطبّق قيمتها النهائية، واستدعاؤها في بداية `checkout`، `holdCurrentSale`، `removeItem`، و`clear`.

### 1.3 خصم سالب يرفع سعر الفاتورة بدلاً من خصمه
- **المشكلة:** حقل "مبلغ الخصم" لا يمنع القيم السالبة برمجياً (فقط `min={0}` كخاصية HTML لا تمنع اللصق/التعديل المباشر). بما أن الإجمالي = `Math.max(subtotal - discountAmount, 0)`، فإن خصماً سالباً يرفع الإجمالي بدلاً من خفضه.
- **الملف/السطر:** `app/(dashboard)/pos/page.tsx:327`, `types/pos.ts:85-89`
- **مستوى الخطورة:** 🟡 متوسط
- **طريقة الإصلاح المقترحة:** فرض `Math.max(0, Number(value) || 0)` عند التخزين، وتحديد حد أقصى للخصم لا يتجاوز `subtotal`.

### 1.4 لا توجد معالجة أو رسالة خطأ للكاشير عند فشل الدفع
- **المشكلة:** `handleConfirmPayment` يستدعي `checkout(...)` بدون `try/catch`. عند فشل الاتصال/Supabase أثناء الدفع، لا تظهر أي رسالة للكاشير، ونافذة الدفع تبقى بحالة غير واضحة — الكاشير لا يعرف هل نجحت العملية أم لا.
- **الملف/السطر:** `app/(dashboard)/pos/page.tsx:120-133`
- **مستوى الخطورة:** 🔴 حرج
- **طريقة الإصلاح المقترحة:** لف الاستدعاء بـ `try/catch` وعرض رسالة خطأ واضحة (Toast) مع إبقاء نافذة الدفع مفتوحة للسماح بإعادة المحاولة. (ملاحظة إيجابية: السلة لا تُفرَّغ إلا بعد نجاح فعلي، فلا خطر ضياع بيانات السلة عند الفشل.)

### 1.5 مسح الكاميرا قد يلتقط نفس الباركود أكثر من مرة
- **المشكلة:** عند اكتشاف باركود بالكاميرا، يُستدعى `onDetected` مباشرة دون إيقاف حلقة القراءة (`controls.stop()`) أولاً، ما يسمح بالتقاط نفس الباركود مرة أو مرتين إضافيتين خلال فجوة زمنية قصيرة قبل إغلاق النافذة — فتتضاعف الكمية المضافة فعلياً لمسح واحد.
- **الملف/السطر:** `components/features/shared/CameraBarcodeScanner.tsx:38-43`
- **مستوى الخطورة:** 🟡 متوسط
- **طريقة الإصلاح المقترحة:** استدعاء `controls.stop()` فور نجاح الاكتشاف وقبل `onDetected`، مع علم محلي (`hasDetectedRef`) يمنع معالجة أي نتيجة لاحقة من نفس الحلقة.

### ✅ تم التحقق (لا توجد مشكلة)
- **منع الضغط المزدوج على "تأكيد الدفع":** الزر معطّل بشكل صحيح أثناء `isCheckingOut` (`usePOS.ts:261`).
- **ذرّية خصم/إرجاع المخزون:** تتم عبر RPC ذرية واحدة (`adjust_product_stock`)، لا يمكن فرط البيع حتى مع تزامن حقيقي.
- **مسار عدم الاتصال بالإنترنت:** يوجد نظام Offline-first متكامل (IndexedDB outbox + مزامنة لاحقة)، والبيع الآجل معطّل عمداً أثناء الانقطاع لأنه يحتاج قراءة حية لرصيد العميل — قرار تصميمي مقصود.
- **المخزون الصفري وسلة التعليق:** التحقق من توفر الكمية يتم فعلياً عند لحظة الإضافة (وليس فقط عند تحميل الصفحة).

---

## 2. رحلة الكاشير (Cashier / POS Journey)

### 2.1 لا يوجد حد أعلى لمبلغ الخصم اليدوي — يمكن جعل الفاتورة مجانية بالكامل
- **المشكلة:** حقل الخصم يحتوي فقط `min={0}` بدون `max`. أي خصم يفوق المجموع الفرعي يُصفّر الفاتورة بصمت دون أي تحذير، و`createSale` لا يعيد التحقق من `discountAmount` مقابل `subtotal` إطلاقاً قبل تسجيل البيع في قاعدة البيانات.
- **الملف/السطر:** `app/(dashboard)/pos/page.tsx:319-328`, `services/sales.service.ts:203-273`
- **مستوى الخطورة:** 🔴 حرج
- **طريقة الإصلاح المقترحة:** إضافة `max={totals.subtotal}` بالواجهة، وتحقق صارم داخل `createSale` يرفض أي `discountAmount` سالب أو أكبر من `subtotal`.

### 2.2 مبلغ الاسترجاع غير مُتحقق منه مقابل سعر البيع الأصلي — ثغرة احتيال مالي مباشر
- **المشكلة:** حقل "قيمة الاسترجاع" حر تماماً ويمكن للكاشير تعديله يدوياً لأي قيمة موجبة. `recordReturn` يتحقق فقط من أن الكمية لا تتجاوز المتبقي القابل للإرجاع، لكنه **لا يتحقق أبداً** أن `refundAmount` يطابق أو لا يتجاوز `unit_price × quantity` الفعلي للفاتورة الأصلية. كاشير يمكنه تسجيل استرجاع بمبلغ مبالغ فيه وسحبه نقداً من الدرج.
- **الملف/السطر:** `services/returns.service.ts:47-101`, `components/features/sales/SaleReturnPanel.tsx:223-233`
- **مستوى الخطورة:** 🔴 حرج
- **طريقة الإصلاح المقترحة:** في `recordReturn`، جلب `unit_price` الفعلي من `sale_items` والتحقق أن `refundAmount <= unit_price × quantity`، ورفض أي قيمة أعلى إلا بموافقة أدمن صريحة مسجّلة.

### 2.3 سباق تحقق-ثم-تنفيذ في تسجيل الاسترجاع قد يسمح باسترجاع مضاعف
- **المشكلة:** `recordReturn` يقرأ مجموع المرتجعات الحالية ثم يُدرج سطر الإرجاع الجديد في استعلام منفصل، دون قيد ذري يربط القراءة بالإدراج. ضغطتان سريعتان (أو تبويبان مفتوحان لنفس الفاتورة) يمكن أن تجتازا الفحص كلتاهما قبل اكتمال أي إدراج، فيتم استرجاع نفس الكمية مرتين مع زيادة المخزون مرتين.
- **الملف/السطر:** `services/returns.service.ts:53-89`
- **مستوى الخطورة:** 🔴 حرج
- **طريقة الإصلاح المقترحة:** نقل التحقق والإدراج إلى دالة RPC ذرية واحدة (على غرار `adjust_product_stock`)، أو إضافة قيد/trigger بقاعدة البيانات يمنع تجاوز مجموع الكميات المرتجعة للحد الأقصى الأصلي.

### 2.4 مسح الباركود ممكن قبل فتح الوردية أصلاً
- **المشكلة:** `ShiftGate` يمنع فقط التفاعل بالماوس عبر نافذة تغطية، لكن قارئ الباركود الفعلي (HID) يرسل ضغطات مفاتيح تصل لمستمع عام على `window` غير مرتبط بحالة الوردية إطلاقاً. يمكن مسح منتجات وخصم كميتها من المخزون فعلياً دون وجود وردية مفتوحة، ولا يوجد أي تحقق من وجود وردية داخل `checkout`/`scanBarcode` بتاتاً.
- **الملف/السطر:** `components/features/pos/BarcodeScanner.tsx:40-51`, `hooks/useBarcodeScanner.ts:29-60`, `components/features/pos/ShiftGate.tsx:27-39`
- **مستوى الخطورة:** 🟡 متوسط
- **طريقة الإصلاح المقترحة:** تعطيل مستمع الباركود عندما لا توجد وردية مفتوحة (`enabled: Boolean(shift)`)، أو إضافة تحقق صريح من وجود وردية داخل `scanBarcode`/`checkout`/`holdCurrentSale`.

### 2.5 عدم تعطيل زر الطباعة أثناء تحميل بيانات المتجر (تحسين)
- **المشكلة:** يمكن الضغط على "طباعة الفاتورة" قبل اكتمال جلب بيانات المتجر (اسم/هاتف/عنوان)، فتُطبع الفاتورة ببيانات فارغة. لا مخاطرة مالية، لكنها مشكلة تجربة استخدام واردة الحدوث فعلياً.
- **الملف/السطر:** `components/features/pos/ReceiptPrinter.tsx:14-15, 85-86`
- **مستوى الخطورة:** 🟢 تحسين
- **طريقة الإصلاح المقترحة:** تعطيل زر الطباعة (`disabled={isLoading}`) حتى اكتمال تحميل بيانات المتجر.

### 2.6 رسالة خطأ غير واضحة عند تعارض فتح وردية متزامن (تحسين)
- **المشكلة:** عند حدوث سباق فعلي بفتح الوردية (نقرتين سريعتين)، خط الدفاع بقاعدة البيانات يمنع فتح ورديتين بنجاح (آمن)، لكن رسالة الخطأ الظاهرة للكاشير عامة وغير مفهومة.
- **الملف/السطر:** `services/shifts.service.ts:31-66`
- **مستوى الخطورة:** 🟢 تحسين
- **طريقة الإصلاح المقترحة:** التقاط كود خطأ `23505` (تكرار مفتاح) وإعادة محاولة جلب الوردية المفتوحة بدل رمي خطأ عام.

### ✅ تم التحقق (لا توجد مشكلة)
- **إخفاء المبلغ المتوقع أثناء عد النقدية (منع الاحتيال):** الحساب يتم بالكامل على الخادم، لا يصل للمتصفح — الحماية سليمة وغير متراجعة.
- **تسجيل الخروج التلقائي بعد إغلاق الوردية:** يعمل بشكل صحيح.
- **مخزون سباق المسح السريع:** RPC ذرية تمنع نزول المخزون تحت الصفر حتى مع مسح متزامن حقيقي.
- **استرجاع كمية أكبر من المباعة:** يوجد تحقق على مستوى الواجهة والخدمة يمنع ذلك (بعكس مبلغ الاسترجاع، انظر 2.2).
- **استئناف نفس الفاتورة المعلّقة من كاشيرين:** عملية حذف ذرية على مستوى الصف تمنع الاستئناف المزدوج.
- **النقر المزدوج على "دفع":** الزر معطّل بشكل صحيح أثناء المعالجة.
- **مزامنة المبيعات غير المتصلة (Outbox):** إعادة المحاولة تستخدم نفس المفتاح الأساسي، فلا يمكن إنشاء بيع مكرر.

---

## 3. رحلة المدير (Admin Journey)

### 3.1 الكاشير يقدر يحذف/يعدّل المنتجات والأقسام مباشرة (لا يوجد تحقق دور بقاعدة البيانات)
- **المشكلة:** سياسة RLS لجدولي `products` و`categories` تسمح لأي مستخدم مسجّل دخول (بما فيهم كاشير) بالحذف والتعديل والإضافة طالما `store_id` مطابق — بدون أي شرط على الدور. الواجهة تخفي فقط أزرار استلام/تلف/تسوية المخزون عن الكاشير، لكن أزرار "تعديل"/"حذف" المنتج ظاهرة للجميع، ولا توجد أي طبقة خدمة (service) أو خادم تتحقق من الدور قبل التنفيذ. **(نفس البند تفصيلياً في قسم "الأمان" رقم 4.1 — مذكور هنا لأنه يمس صلاحيات إدارة المخزون مباشرة).**
- **الملف/السطر:** `supabase/migrations/00000000000012_multi_tenancy_foundation.sql:210-212`, `components/features/inventory/StockTable.tsx:92-104`, `app/(dashboard)/inventory/page.tsx:73-78`
- **مستوى الخطورة:** 🔴 حرج
- **طريقة الإصلاح المقترحة:** إضافة شرط دور (admin) على عمليات update/delete لجدولي products وcategories في RLS، مع إخفاء الأزرار عن الكاشير بالواجهة كطبقة دفاع إضافية فقط (ليست الحماية الأساسية).

### 3.2 لا يوجد تحقق من عدم سماح أسعار سالبة أو بيع بخسارة على مستوى قاعدة البيانات
- **المشكلة:** عمودا `cost_price`/`sale_price` بجدول `products` بدون أي `check constraint` (بعكس `product_units.sale_price` الذي يحمل `check >= 0`). الواجهة تمنع فقط سعر بيع `<= 0`، ويظهر تحذير بصري فقط (وليس منعاً) عند بيع بخسارة (`sale <= cost`).
- **الملف/السطر:** `supabase/migrations/00000000000000_init.sql:25-26`, `components/features/inventory/ProductForm.tsx:67-70,86-88`
- **مستوى الخطورة:** 🟡 متوسط
- **طريقة الإصلاح المقترحة:** إضافة `check (cost_price >= 0)` و`check (sale_price >= 0)` عبر migration جديد، ومنع الحفظ فعلياً (لا تحذير فقط) عند سعر تكلفة سالب.

### 3.3 تقرير المبيعات: نطاق التاريخ المخصص بتبويبي "الاتجاه" و"الترتيب" لا يُطبَّق عليه تصحيح نهاية اليوم — انحدار (Regression) لخلل تم إصلاحه سابقاً
- **المشكلة:** ميزة تصدير Excel (`SalesExportModal.tsx`) تصحح تاريخ النهاية إلى نهاية اليوم (`23:59:59.999`) بشكل صحيح (تم إصلاحها بجلسة سابقة). لكن صفحة `/sales` نفسها، في تبويبي "الاتجاه" و"الترتيب"، تبني تاريخ النهاية من حقل `<input type="date">` مباشرة **بدون** نفس التصحيح، فتُقطع كل تقارير المدى المخصص (وليس preset) عند بداية يوم النهاية تقريباً بدلاً من نهايته — يستبعد معظم مبيعات آخر يوم من كل تقارير الاتجاه والترتيب (أقسام/منتجات/كاشير) عند اختيار مدى مخصص.
- **الملف/السطر:** `app/(dashboard)/sales/page.tsx:90`, `hooks/useSalesAnalytics.ts:11-12` (مقارنة بالإصلاح الصحيح في `components/features/sales/SalesExportModal.tsx:35-41`)
- **مستوى الخطورة:** 🔴 حرج
- **طريقة الإصلاح المقترحة:** تطبيق نفس منطق `toExportRange` (تصحيح نهاية اليوم) داخل `handleCustomRangeChange`/`resolveRange`. يُفضّل استخراج الدالة كمنطق مشترك واحد في `lib/` يستوردها كل من `SalesExportModal` وصفحة `/sales` لمنع تكرار/نسيان هذا المنطق مستقبلاً.

### 3.4 سبب النقص بالتسوية (تسوية الجرد) غير إلزامي على مستوى الخدمة (تحسين)
- **المشكلة:** `recordReconciliation` تقبل `reason: string | null` بدون تحقق من إلزاميته عند وجود نقص (shortage) — والتحقق الوحيد موجود بالواجهة فقط، ما يعني أن أي استدعاء مباشر لدالة الخدمة (خارج النموذج الحالي) يمكن أن يسجّل نقص مخزون بدون أي سبب موثّق.
- **الملف/السطر:** `services/reconciliations.service.ts:8-13,25-30`
- **مستوى الخطورة:** 🟢 تحسين
- **طريقة الإصلاح المقترحة:** في `recordReconciliation`، إذا كان الفرق سالباً (نقص)، اشترط وجود `reason` غير فارغ وارمِ خطأ عربي واضح إن لم يوجد.

### ✅ تم التحقق (لا توجد مشكلة)
- **الحذف الناعم:** المنتجات والوحدات والأقسام والموردين كلها soft-delete (`is_active=false`)، لا يوجد أي حذف فعلي يهدد أي مرجع مالي/تاريخي.
- **تفرد الباركود بعد الحذف:** تم إصلاحه سابقاً (migration 19) ولا يوجد أي مسار لاحق يعيد فرض القيد القديم.
- **حد 90 يوماً على التقارير (MAX_RANGE_DAYS):** مطبّق فعلياً على جميع نقاط دخول التقارير الخمس.
- **لا يوجد N+1:** جميع عمليات البحث المجمّعة (cashier/product/category lookups) تستخدم استعلام واحد مجمّع.
- **مسار إدارة الموظفين:** حماية صحيحة (منع تعديل الذات، فحص الدور خادمياً، فحص المتجر قبل الإدارة، كلمة مرور بحد أدنى وبدون أي تسجيل/طباعة).
- **مدفوعات ومشتريات الموردين:** تحقق صحيح يمنع مبالغ سالبة أو دفعات تتجاوز الرصيد المستحق.
- **تسجيل التلف (Damage):** كمية صحيحة موجبة ولا تتجاوز المخزون المتوفر.

---

## 4. الأمان والبنية التحتية (Backend & Supabase)

### 4.1 دالة `handle_new_user` لا تتحقق من وجود المتجر (store_id) قبل الوثوق به — احتمال تصعيد صلاحيات
- **المشكلة:** الدالة تأخذ `store_id` مباشرة من `raw_user_meta_data` (بيانات يرسلها الطالب نفسه عبر Supabase Auth signup) دون التحقق من وجود صف فعلي بهذا المعرّف في جدول `stores`. إذا لم يوجد أي موظف بنفس `store_id` من قبل، يُمنح المستخدم الجديد دور **أدمن** تلقائياً. **أي شخص يسجّل حساباً مباشرة عبر Supabase Auth (لو كان التسجيل العام مفعّلاً بلوحة Supabase) بإرسال `store_id` عشوائي يصبح أدمن فوراً لمتجر وهمي أو حتى متجر حقيقي فقد كل موظفيه.**
- **الملف/السطر:** `supabase/migrations/00000000000012_multi_tenancy_foundation.sql:287-305`
- **مستوى الخطورة:** 🔴 حرج
- **طريقة الإصلاح المقترحة:** (أ) تعديل الدالة للتحقق من وجود `store_id` فعلياً بجدول `stores` قبل قبوله. (ب) **الأهم:** التحقق العاجل من لوحة Supabase Auth وتعطيل التسجيل العام (public signup) نهائياً — المسار الوحيد الموجود فعلياً بالكود لإنشاء موظفين هو `/api/employees` (محمي جيداً بفحص أدمن خادمي)، ما يعني أن التسجيل العام غير مقصود أصلاً في هذا التصميم.

### 4.2 لا يوجد تمييز صلاحيات admin/cashier على مستوى RLS لجدول المنتجات والأقسام
- **المشكلة:** (نفس تفاصيل البند 3.1) — سياسة RLS الحالية تسمح لأي مستخدم مسجّل بنفس المتجر بالتعديل/الحذف الكامل على `products`/`categories` بدون تمييز دور. هذا يعني أن أي تحقق "أدمن فقط" في الواجهة (React) هو تجميل بصري لا حماية فعلية، لأن استدعاءً مباشراً لواجهة Supabase (حتى من متصفح كاشير عادي عبر console) ينجح دون عائق.
- **الملف/السطر:** `supabase/migrations/00000000000012_multi_tenancy_foundation.sql:210-212`, `services/products.service.ts:191-198`, `components/features/inventory/ReceiveStockForm.tsx:26`
- **مستوى الخطورة:** 🟡 متوسط
- **طريقة الإصلاح المقترحة:** إضافة فحص دور صريح داخل دوال الخدمة الحساسة (`updateProduct`, `recordStockPurchase`, حذف/تعديل) بجلب دور المستخدم من `profiles` قبل التنفيذ، أو تقييد RLS بشرط دور، أو استخدام دالة RPC بصلاحية `security definer` تتحقق من الدور صراحة.

### 4.3 استعلام سجل العمليات (الأرشيف) بدون حد أقصى أو تقسيم صفحات
- **المشكلة:** `listOperations()` تُنفّذ `select("*")` بدون `.limit()`/`.range()` على جدول يتراكم فيه صف جديد مع كل عملية بيع/تعديل/تسوية. بعد أشهر من الاستخدام الفعلي، سيجلب الاستعلام عشرات آلاف الصفوف دفعة واحدة، ما يُبطئ صفحة الأرشيف بشدة أو يُعلّقها.
- **الملف/السطر:** `services/archive.service.ts:54`
- **مستوى الخطورة:** 🟡 متوسط
- **طريقة الإصلاح المقترحة:** إضافة `.range()`/`.limit()` مع ترقيم صفحات بالواجهة، أو فرض نطاق تاريخ افتراضي محدود (مثلاً آخر 30 يوماً) إن لم يُحدَّد فلتر.

### 4.4 الميدل وير يفشل بشكل "مفتوح" عند تعذر التحقق من حالة المتجر (تحسين — سلوك مقصود وموثّق)
- **المشكلة:** عند فشل فحص حالة المتجر (خطأ شبكة مثلاً)، يُسمح بالمرور افتراضياً. تم التحقق أن هذا **قرار مقصود وموثّق** لتفادي إيقاف متجر نشط بسبب عطل مؤقت، والحماية الفعلية (fail-closed) موجودة فعلياً على مستوى RLS عبر `current_store_id()` بغض النظر عن الميدل وير — فلا توجد ثغرة أمنية حقيقية هنا.
- **الملف/السطر:** `lib/supabase/middleware.ts:61-97`
- **مستوى الخطورة:** 🟢 تحسين
- **طريقة الإصلاح المقترحة:** لا حاجة لإصلاح أمني؛ يُقترح فقط إضافة تسجيل تنبيه (alert) عند تكرار فشل هذا الفحص لتفادي إخفاء أعطال متكررة بالإنتاج.

### ✅ تم التحقق (لا توجد مشكلة)
- **تغطية RLS الشاملة:** كل الجداول عبر جميع الترحيلات (20 migration) لديها RLS مفعّل وسياسات صحيحة؛ لا توجد أي `using (true)` متبقية فعلياً في الحالة النهائية للسياسات.
- **`current_store_id()`:** تفشل بشكل صحيح ومغلق (fail-closed) لأي متجر موقوف أو مستخدم بلا متجر.
- **مسار إدارة الموظفين (API):** الوحيد الذي يستخدم عميل service-role، ومحمي جيداً بفحص أدمن خادمي وفحص تطابق المتجر.
- **كلمة المرور:** حد أدنى مفروض، لا تُسجَّل أو تُطبع في أي مكان، ولا يوجد مسار "نسيت كلمة المرور" ذاتي الخدمة يفتح ثغرة إضافية.

---

## خلاصة وترتيب الأولوية للتنفيذ

**يُنصح بالتعامل مع البنود الحرجة (🔴) بالترتيب التالي:**

1. الأمان 4.1 (تحقق store_id + تعطيل التسجيل العام) — أعلى أولوية لأنها تمس كل المتجر، ويحتاج فحص إعدادات Supabase أولاً.
2. الكاشير 2.2 و2.3 (احتيال الاسترجاع) — مالي مباشر، يُنصح بالتحقيق فوراً في أي عمليات استرجاع سابقة مشبوهة أثناء الإصلاح.
3. المدير/الأمان 3.1/4.2 (صلاحيات المنتجات) — سهل الاستغلال من أي كاشير حالي.
4. المدير 3.3 (خلل تقارير المبيعات بالمدى المخصص) — يؤثر على دقة الأرقام المالية التي يعتمد عليها صاحب المتجر لاتخاذ قرارات.
5. الكاشير 2.1 (الخصم بدون حد أعلى) و الزبون 1.4 (رسالة خطأ الدفع).
6. باقي البنود الحرجة (1.1، 1.2 المرتبطة بها) حسب الأولوية التشغيلية.

البنود المتوسطة والتحسينية يمكن جدولتها كدفعات لاحقة (batches) بنفس أسلوب "العميلين خارقي الذكاء" المعتمد بهذا المشروع — كل بند أو مجموعة بنود مترابطة تمر بنفس دورة brainstorming → خطة → تنفيذ → تحقق.
