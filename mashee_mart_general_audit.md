# تقرير التدقيق الشامل — ماشي مارت (Comprehensive Code Audit)

**تاريخ التدقيق:** 2026-09-09
**نوع التقرير:** فحص أخطاء برمجية ومنطقية فقط (لا يشمل الثغرات الأمنية — سيتم تناولها في مرحلة لاحقة منفصلة)
**طريقة العمل:** فريق من 3 عملاء فحص (QA Agents) متخصصين، كل منهم فحص الكود الفعلي وتتبّع المسارات الحرجة قبل الإبلاغ عن أي خطأ. هذا التقرير للقراءة فقط، لم يتم تعديل أي كود.

**ملخص الأولويات:**

| المستوى | عدد المشاكل |
|---|---|
| 🔴 عالي | 4 |
| 🟠 متوسط | 4 |
| 🟡 منخفض | 4 |

---

## العميل الأول: مدقق الواجهات وإدارة الحالة (UI & State Manager)

**نطاق الفحص:** مكونات React، إدارة الحالة عبر React Context (`AuthContext`، `POSContext`، `OfflineContext`) وHooks، إعادة التصيير، فقدان البيانات بين الصفحات، تزامن حالة Offline/Online في الواجهة.

### 1. تكرار مراقبة حالة الاتصال بالإنترنت في نسختين منفصلتين ومتعارضتين

**الملف والسطر:** `hooks/usePOS.ts:30` (مقارنة بـ `context/OfflineContext.tsx:23`)
**مستوى الخطورة:** 🔴 عالي
**الوصف:** يستدعي `usePOS` دالة `useOnlineStatus()` بشكل مستقل تمامًا عن `OfflineContext`، رغم أن التعليق التوثيقي في `context/OfflineContext.tsx:21` يذكر صراحة أن `OfflineProvider` وُضع بين `AuthProvider` و`POSProvider` لأن "usePOS needs isOnline" — أي أن النية الأصلية كانت أن يستهلك `usePOS` قيمة `isOnline` من `OfflineContext` وليس أن ينشئ نسخة ثانية خاصة به. كل نسخة من `useOnlineStatus` تُشغّل دورة فحص (probe) واستماع لأحداث `online`/`offline` بشكل مستقل تمامًا (مؤقتات ومراجع منفصلة)، لذا يمكن أن تتباين قيمة `isOnline` بين النسختين لبضع ثوانٍ خصوصًا عند إعادة الاتصال. النتيجة: قد يعرض الكاشير منتجات محدّثة فعليًا من الخادم بينما تُخصم الكمية من نسخة الكاش المحلي القديم (أو العكس)، مما يؤدي إلى تضارب في المخزون أو تسجيل عملية بيع أوفلاين رغم توفر الاتصال فعليًا.
**طريقة الإصلاح المقترحة:** تعديل `usePOS` ليستقبل `isOnline` كوسيط (parameter) بدلاً من استدعاء `useOnlineStatus()` بشكل مستقل، بحيث يمرره `POSProvider` من `useOfflineContext()` (مصدر الحقيقة الوحيد)، وإزالة أي استدعاء مستقل آخر لـ `useOnlineStatus` خارج `OfflineContext`.

### 2. عدم تطابق مصدر بيانات الكتالوج بين شاشة الباركود اليدوية وحالة usePOS الفعلية

**الملف والسطر:** `components/features/pos/BarcodeScanner.tsx:34,53-74`
**مستوى الخطورة:** 🟠 متوسط
**الوصف:** امتداد مباشر للمشكلة أعلاه: نتيجة الاعتماد على مصدرين مختلفين لـ `isOnline`، قد تُحمَّل قائمة المنتجات/الفئات في `BarcodeScanner` من Supabase الحي بينما `usePOS.addProductToCart` (الذي يستخدم نسخته الخاصة من `isOnline` وقد لا تكون قد تحدثت بعد) يتعامل مع الكمية كأنها أوفلاين، فيقرأ من كاش قديم محتمل بدلاً من الرصيد الحقيقي، مما قد يسمح بإضافة كمية غير متوفرة فعليًا للسلة أو يرفض كمية متوفرة فعليًا خطأً.
**طريقة الإصلاح المقترحة:** ستُحل تلقائيًا بعد توحيد مصدر `isOnline` (إصلاح البند 1) لأن كل من `BarcodeScanner` و`usePOS` سيعتمدان على نفس القيمة من `OfflineContext`.

**مناطق فُحصت ولم يُعثر فيها على مشاكل حقيقية:** إعادة تصيير موفّري `AuthContext`/`POSContext` (قيمة provider جديدة بكل تصيير موجودة لكن غير مؤثرة عمليًا بسبب ندرة أحداث المصادقة)، فقدان بيانات السلة/الفواتير المعلّقة عند التنقل بين الصفحات.

---

## العميل الثاني: خبير المنطق التجاري (Business Logic & Math Expert)

**نطاق الفحص:** السلة، الكاشير (POS)، نظام الجرد — دقة الحسابات (الجمع، الضرب، الخصومات)، ومنطق إضافة الكميات ومطابقتها للمخزون.

### 3. لا يتم تخفيض رصيد دين الزبون عند إرجاع منتج من فاتورة بيع آجل

**الملف والسطر:** `supabase/migrations/00000000000021_atomic_return_recording.sql:123-151` (دالة `record_return`)، `services/returns.service.ts:53-90`، `supabase/migrations/00000000000011_customer_debt_management.sql:23`
**مستوى الخطورة:** 🔴 عالي
**الوصف:** عند إرجاع صنف من فاتورة بيع بالآجل (عبر `SaleReturnPanel.tsx` الذي لا يتحقق إطلاقًا من طريقة الدفع)، تُسجَّل عملية الإرجاع في جدول `returns` ويُعاد المخزون، لكن لا يوجد أي إدخال في `customer_transactions` لتخفيض دين الزبون. بما أن رصيد الزبون يُحسب فقط من فرق (sale − payment)، يبقى الزبون مديناً بكامل قيمة الفاتورة الأصلية رغم إرجاعه للبضاعة، مما يؤدي لمطالبته بمبلغ أكبر من المستحق فعليًا.
**طريقة الإصلاح المقترحة:** إضافة نوع معاملة جديد (مثلاً `'return'`) إلى قيد التحقق على `customer_transactions.type`، وتعديل دالة `record_return` بحيث تُدرج تلقائيًا (عند وجود `customer_id` مرتبط بالفاتورة الأصلية) قيدًا يُخفّض الرصيد بمقدار `refund_amount`.

### 4. تقرير أداء الكاشيرين لا يخصم قيمة الإرجاعات أو التوالف من الربح، بعكس بقية التقارير

**الملف والسطر:** `services/sales.service.ts:1158-1276` (خصوصًا 1252-1273؛ قارن مع `getProductRanking` أسطر 806-845 وَ`getDailySalesSummary` أسطر 310-317)
**مستوى الخطورة:** 🟠 متوسط
**الوصف:** كل من `getDailySalesSummary`، `getProductRanking`، `getCategoryRanking`، و`getSalesTrend` يطرحون قيمة الإرجاعات والتوالف والتسويات من الربح الإجمالي، لكن `getCashierRanking` لا يفعل ذلك إطلاقًا رغم أن التوثيق الداخلي (JSDoc) يزعم أنه "نفس getProductRanking". النتيجة أن ربح الكاشير المعروض يكون مبالغًا فيه بشكل ممنهج مقارنة بتقرير المنتج/الفئة/اليوم لنفس الفترة.
**طريقة الإصلاح المقترحة:** توحيد المنطق: طرح صافي أثر الإرجاعات/التوالف/التسويات (لكل كاشير عبر `actor_id`) من `totalProfit` في `getCashierRanking` بما يطابق بقية دوال هذا الملف، أو توضيح الفرق بشكل صريح في التوثيق إن كان سلوكًا مقصودًا.

### 5. زر زيادة الكمية (+) في السلة لا يتحقق من المخزون المتاح قبل الإرسال

**الملف والسطر:** `components/features/pos/CartGrid.tsx:43-50`، `hooks/usePOS.ts:221-256` (`updateQuantity`)، `types/pos.ts:19` (`availableStock` معرّف لكن غير مستخدم فعليًا)
**مستوى الخطورة:** 🟡 منخفض
**الوصف:** الضغط على زر + يرفع الكمية المعروضة فورًا دون أي سقف من جهة العميل، ولا يتحقق المخزون الفعلي إلا بعد مهلة تجميع (debounce) مدتها 200ms عبر خصم ذري من الخادم، ثم يتم التراجع (rollback) إذا فشل. توجد نافذة زمنية قصيرة تُظهر فيها السلة كمية تفوق المخزون الفعلي قبل التصحيح.
**طريقة الإصلاح المقترحة:** إضافة سقف تفاعلي بسيط في `CartGrid` يعطل زر + عند الوصول لـ `item.availableStock` المعروف وقت الإضافة، مع الإبقاء على التحقق الذري من الخادم كخط دفاع أخير.

### 6. تضارب محتمل في قيمة الأساس (baseline) عند تعديل كمية جديد أثناء تراجع معلّق

**الملف والسطر:** `hooks/usePOS.ts:163-192` (`applyPendingAdjustment`) و`221-256` (`updateQuantity`)
**مستوى الخطورة:** 🟡 منخفض
**الوصف:** عند فشل خصم المخزون ضمن `applyPendingAdjustment` يتم استدعاء تراجع غير متزامن. إذا ضغط الكاشير على + مجددًا لنفس الصنف أثناء تنفيذ هذا التراجع، فقد يقرأ `updateQuantity` الجديد قيمة أساس (baseline) خاطئة من الحالة الحالية غير المُصحَّحة بعد، مما قد يُسبب طلب كمية غير صحيحة.
**طريقة الإصلاح المقترحة:** جعل `applyPendingAdjustment` يمسك قفلاً بسيطًا لكل باركود أثناء تنفيذ التراجع، بحيث يُرفض أو يُؤجَّل أي `updateQuantity` جديد لنفس الصنف حتى انتهاء التراجع الحالي.

### 7. عدم تقريب المجاميع في مسار البيع دون اتصال (offline) بعكس الخادم

**الملف والسطر:** `types/pos.ts:85-89` (`calculateTotals`)، `hooks/usePOS.ts:356-397`، مقارنةً بمنطق التقريب في `supabase/migrations/00000000000027_atomic_sale_recording.sql`
**مستوى الخطورة:** 🟡 منخفض
**الوصف:** أي سعر يحمل أكثر من رقمين عشريين (كما قد يحدث مع معامل تحويل الوحدات) يُنتج فرقًا بين الإيصال المعروض فورًا للزبون أثناء انقطاع الاتصال (غير مقرّب) والقيمة النهائية المخزَّنة بعد المزامنة (مقرّبة لرقمين). لا يؤثر على صحة السجل النهائي، لكن قد يجعل الإيصال المطبوع مختلفًا عن الفاتورة المسجَّلة لاحقًا.
**طريقة الإصلاح المقترحة:** تطبيق تقريب لرقمين عشريين على نتائج `calculateTotals` في مسار عرض الإيصال دون اتصال، بما يطابق منطق التقريب في `create_sale_atomic`.

**مناطق فُحصت ولم يُعثر فيها على مشاكل حقيقية:** جميع الدوال الذرية في قاعدة البيانات (`create_sale_atomic`، `adjust_product_stock`، `receive_product_stock`، `record_return`، `record_reconciliation`، `close_shift_atomic`)، تحويلات الوحدات (كرتونة/وحدة أساسية)، مسارات إضافة الصنف الثلاثة (HID/كاميرا/يدوي) من ناحية عدم تجاوز المخزون الفعلي، صافي الربح في التقارير اليومية/المنتج/الفئة، حساب الخصم.

---

## العميل الثالث: مفتش الربط والمزامنة (Integration & Sync Inspector)

**نطاق الفحص:** الاتصال بـ Supabase، وضوح رسائل الخطأ للمستخدم، دقة نظام مزامنة الفواتير غير المتصلة (Offline Outbox).

### 8. عملية بيع تعلق للأبد بحالة "syncing" عند فشل المزامنة ولا يُعاد جدولتها أبدًا

**الملف والسطر:** `lib/offline/syncManager.ts:48-80` (تحديدًا 49-50 مقابل catch في 75-80)، بالتفاعل مع `lib/offline/outbox.ts:30-34`
**مستوى الخطورة:** 🔴 عالي
**الوصف:** قبل محاولة مزامنة أي عملية بيع، تُوضع علامة `status: "syncing"` وتُحفظ فورًا في IndexedDB. إذا فشلت `createSale` أو `decrementStockForSale` لاحقًا (انقطاع شبكة، خطأ تكرار، أو أي خطأ آخر)، فإن `catch` لا يفعل شيئًا سوى `break` — التعليق يزعم أن العملية تبقى "pending" لكن هذا غير صحيح فعليًا: لا كود يعيد الحالة إلى "pending". بما أن حلقة المزامنة تفلتر فقط على `status === "pending"`، تبقى هذه العملية عالقة بحالة "syncing" إلى الأبد ولن تُعاد محاولتها مطلقًا، ولن تظهر في نافذة التعارضات لأنها ليست "conflict". نفس المشكلة تمامًا تحدث في حلقة الفواتير المعلّقة (`held_sales`). يحدث هذا أيضًا لو أُغلق التطبيق تمامًا بعد وضع "syncing" وقبل اكتمال الشبكة — الحالة محفوظة بشكل دائم دون أي كود عند إعادة التشغيل يعيدها إلى "pending".
**طريقة الإصلاح المقترحة:** في كتلة `catch`، أعد الحالة إلى "pending" قبل `break`. أضف أيضًا دالة `resetStaleSyncing` تُستدعى عند بدء تشغيل `OfflineProvider` لإعادة أي عنصر عالق بحالة "syncing" من جلسة سابقة انهارت إلى "pending".

### 9. فقدان صامت للمخزون المخصوم دون تسجيل عملية بيع مقابلة

**الملف والسطر:** `lib/offline/syncManager.ts:52-80`
**مستوى الخطورة:** 🔴 عالي
**الوصف:** الترتيب هو: خصم المخزون الحقيقي عبر RPC ذري، ثم `createSale`. إذا نجح خصم المخزون لكن `createSale` فشلت (سجل مكرر، أو انقطاع شبكي بعد نجاح RPC وقبل استلام الرد)، يقع الكود في `catch` ويتوقف. بما أن هذه العملية تبقى عالقة على "syncing" إلى الأبد (البند 8)، يبقى المخزون الحقيقي منقوصًا في قاعدة البيانات دون أي سجل بيع مقابل، ولا توجد أي آلية تكشف هذا التضارب لاحقًا — خسارة مخزون صامتة تمامًا.
**طريقة الإصلاح المقترحة:** بالإضافة لإصلاح البند 8، عند فشل `createSale` تحديدًا بعد نجاح خصم المخزون، سجّل هذا كحالة خاصة (مثلاً "partial") وأظهرها للمستخدم بوضوح كي يُراجع المخزون يدويًا.

### 10. لا يوجد تعامل مع رمز الخطأ 23505 (تكرار المفتاح) عند إعادة محاولة مزامنة فاتورة مُزامَنة فعليًا

**الملف والسطر:** `lib/offline/syncManager.ts:75, 111` مقابل `supabase/migrations/00000000000027_atomic_sale_recording.sql:201-212`
**مستوى الخطورة:** 🟠 متوسط
**الوصف:** `p_client_sale_id` (المُنشأ من `crypto.randomUUID()` في `usePOS.ts`) يعمل فعليًا كمفتاح تمييز (idempotency key) يمنع الإدراج المزدوج الفعلي في القاعدة (جيد). لكن عند إعادة محاولة عملية بيع سبق أن نجحت فعليًا على الخادم لكن الرد لم يصل للعميل (انقطاع شبكي بعد الاستجابة)، سيفشل الإدراج بخطأ 23505 غير مُعالج تحديدًا، فيُعامل كأي خطأ آخر ويبقى عالقًا (البند 8) — رغم أن العملية نجحت فعليًا على الخادم. المشروع لديه أصلًا دالة `isUniqueViolation` (`products.service.ts:187`) مستخدمة في أماكن أخرى، لكنها غير مستخدمة هنا إطلاقًا.
**طريقة الإصلاح المقترحة:** عند التقاط الخطأ في `createSale`، تحقق من `isUniqueViolation(err)` — إذا كان الخطأ تكرار مفتاح على `sale.localId`، اعتبر العملية ناجحة فعليًا وضع علامة "synced" بدلًا من تركها عالقة أو إعادة محاولتها بلا نهاية.

### 11. لا يوجد مفتاح تمييز (idempotency) لإدراج الفواتير المعلّقة (held_sales)

**الملف والسطر:** `services/heldSales.service.ts:27-42`، `supabase/migrations/00000000000010_hold_sale.sql:13-20`، `lib/offline/syncManager.ts:96-106`
**مستوى الخطورة:** 🟠 متوسط
**الوصف:** خلافًا لجدول `sales`، جدول `held_sales` يحتوي `id` مُولّد تلقائيًا في الخادم بدون إمكانية تمرير معرّف من العميل. إذا نجح إدراج الصف في الخادم لكن الرد لم يصل، ستقع الحلقة في `catch` وتُترك العملية "syncing" (نفس علة البند 8)، ولا توجد آلية تمنع إدراج نفس الفاتورة المعلّقة مرتين لو أُعيدت المحاولة لاحقًا — ينتج عنها صفان منفصلان لنفس الفاتورة.
**طريقة الإصلاح المقترحة:** إضافة عمود اختياري `client_local_id uuid unique` (nullable) إلى `held_sales`، ومرره من `syncManager.ts` عبر `holdSale`، بنفس أسلوب `sales.id`.

### 12. عدم وجود قفل بين تبويبات المتصفح (cross-tab) يضخّم أثر البنود 8 و10

**الملف والسطر:** `lib/offline/syncManager.ts:21, 34-36`
**مستوى الخطورة:** 🟡 منخفض
**الوصف:** الحارس `isSyncing` هو متغير JS في الذاكرة خاص بنسخة الموديول داخل تبويب واحد فقط — لا يحمي من تشغيل تبويبين مختلفين لنفس المزامنة في نفس اللحظة ضد نفس IndexedDB المشترك. بفضل استخدام `p_client_sale_id` كمفتاح أساسي، لن يحدث بيع مزدوج فعلي في القاعدة، لكن التبويب الخاسر للسباق سيصطدم بخطأ 23505 غير معالج (البند 10) ويعلق للأبد (البند 8).
**طريقة الإصلاح المقترحة:** استخدام `navigator.locks.request(...)` لتنسيق قفل حقيقي بين التبويبات، أو الأبسط: معالجة البندين 8 و10 أولًا فهذا يجعل تصادم التبويبات غير ضار (يتعافى تلقائيًا بدل التعليق الدائم).

**مناطق فُحصت ولم يُعثر فيها على مشاكل حقيقية:** `lib/supabase/client.ts`/`server.ts`/`admin.ts` (فصل صحيح بين الأدوار، لا أخطاء مُبتلعة)، `lib/supabase/middleware.ts` (منطق fail-open سليم وموثّق)، ترتيب تنفيذ المزامنة (FIFO حقيقي حسب `createdAt`)، `useOnlineStatus.ts` (فحص اتصال فعلي عبر ping وليس فقط `navigator.onLine`)، رسائل الأخطاء في طبقة الخدمات بشكل عام (واضحة بالعربية في نقاط القرار المهمة).

---

## الخلاصة

المشاكل الأكثر إلحاحًا للإصلاح هي البنود **1، 3، 8، و9** (جميعها 🔴 عالي): تضارب مصدر حالة الاتصال في الكاشير، عدم تسوية دين الزبون عند الإرجاع، وعلة "التعليق الدائم" في نظام المزامنة غير المتصلة التي قد تُسبب فقدانًا صامتًا للمخزون. يوصى بمعالجتها أولًا قبل الانتقال لمرحلة فحص الثغرات الأمنية.
